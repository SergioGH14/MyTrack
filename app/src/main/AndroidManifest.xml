<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdm.sergio.mytrack">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_moviet"
        android:logo="@drawable/ic_moviet"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_moviet"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ActBottomNav"
            android:screenOrientation="portrait">
            <!-- Bloquear la rotación de la pantalla -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MovieActivity" />
        <activity android:name=".activity.MovieGridGenreActivity"
            android:screenOrientation="portrait">
            <!-- Bloquear la rotación de la pantalla -->
        </activity>
        <activity android:name=".activity.ProfileActivity"
            android:screenOrientation="portrait">
            <!-- Bloquear la rotación de la pantalla -->
        </activity>
        <activity
            android:name=".activity.MyIntro"
            android:theme="@style/FullscreenTheme" />

    </application>

</manifest>